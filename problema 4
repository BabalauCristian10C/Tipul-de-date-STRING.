cnp = str(input("Intoduceti CNP dmnvs. = "))
cifre = ["1","0","2","3","4","5","6","7","8","9"]
a = "Nu este potrivit"
for num in cnp:
    if len(cnp) == 13 and num in cifre:
        a = "OK!"

print(a)
